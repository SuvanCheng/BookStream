# 协作规则（Agent 工作纪律）

## 提交纪律（最高优先级）

- **未经用户明确要求，绝不执行 `git commit`**（同样不主动 `git push`、不 amend、
  不 reset/回滚、不删除提交）。
- 完成改动后停留在工作区（未提交状态），汇报改动内容与验证结果，等待用户指示
  是否提交。用户说「提交」「commit」等明确指令时才执行。

## 文档自驱动更新纪律（核心记忆守护）

- **主动维护 `HANDOFF.md`**：每次完成核心功能改动、重点概念演进、架构重大转向或解决重大踩坑 Bug 后，**必须在汇报前主动更新 `HANDOFF.md`**（补充踩坑经验、更新工作区接力清单、记录核心决策），确保新会话接力时零上下文丢失。
- **主动维护 `AGENTS.md`**：当用户提出新的长期合作习惯、纪律约束或工作流偏好时，必须主动沉淀进 `AGENTS.md`，使后续所有会话中的 Agent 永久继承。

## 其他

- 新会话启动或用户指示「继续」时，优先阅读 `HANDOFF.md`，快速掌握项目全貌、架构演进、用户核心偏好与历史踩坑经验。
- 修改源码前先读取目标文件，避免覆盖用户同时期的改动。
- 涉及验证的改动先构建 + 自检（`swift build`、`--selftest`），确认无警告再汇报。
- 临时验证产物放 /tmp，不污染工作区。
